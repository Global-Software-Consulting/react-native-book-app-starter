export interface IData {
    email: string;
}

export interface UpdateProfile {
    firstName: string;
    lastName: string;
    gender: string;
}
export interface LoginDetail {
    params: string;
}
